# Arch A2A Relay

Balanced Ternary Arch lexicon relay for Agent2Agent (A2A) protocol.

Public endpoint: /a2a/messages (POST)
MIME type: application/x-arch-trit+v5

Deployed on Railway — free & always-on.

Agent card: /.well-known/agent-card.json
